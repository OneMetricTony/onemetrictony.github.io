// Google Apps Script web-app endpoint that persists the shared leaderboard
// (kavosh_leaderboard.json on Tony's Google Drive).
// Empty string = localStorage-only mode (each browser keeps its own copy).
// Paste the /exec URL here after deploying Code.gs (see DEPLOY.md).
window.LB_BACKEND = "";
